Drop the AI-generated hero clip here as: hero.mp4

The hero <video> in index.html points at assets/hero.mp4 (muted, looped, autoplay).
Until a real clip exists, the hero shows a self-contained motion stand-in (cinemagraph),
so the page looks finished for marketing. Adding hero.mp4 makes the video take over
automatically — no code change.

Recommended clip spec (matches the prompt used for the demo):
  - 16:9, 720p+ , 5s loopable, MUTED, no on-screen text
  - Subject: calm sunlit comfortable home interior, gentle airflow / curtains, slow push-in
  - Keep it dim/ambient so the headline stays legible over the scrim

Generated via Higgsfield (kling3_0_turbo) once on a paid plan, or Runway.
